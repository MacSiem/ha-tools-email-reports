{
  "name": "ha-tools-email-reports-smoke-tests",
  "version": "0.0.0-private",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs && node .github/smoke.cjs",
    "test:smoke": "node .github/smoke.cjs",
    "build:check": "node scripts/build-bundle.mjs --check"
  },
  "devDependencies": {
    "jsdom": "24.1.3"
  }
}
